{
  "name": "trackercaps",
  "private": true,
  "version": "1.0.0",
  "description": "Dev tooling for Trackercaps. src/ is the app (ES modules, zero runtime dependencies), served locally via scripts/dev-server.js (`npm run dev`) and packed for publishing via esbuild (`npm run build` -> dist/).",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright": "^1.63.0",
    "pngjs": "^7.0.0"
  }
}
